# docker-compose.yml — yarmirror-db (PostgreSQL + PostGIS) credentials
# Copy this file to .env (which is gitignored) before running `docker compose up -d`.
POSTGRES_DB=yarmirror
POSTGRES_USER=yarmirror
POSTGRES_PASSWORD=yarmirror
